import mysql from 'mysql2/promise'

const database = {
    host: 'localhost',
    database: 'tiket-bot',
    user: 'root',
    password: ''
}



try{
    await 
}catch(error){
    console.error(error);
}
